load("C:\\Users\\Atranemus-PC\\Documents\\R\\191R\\Final Project\\SP_500_2003_2015.data")
ls()
fix(RETS)
RETS2 = na.omit(RETS)
fix(RETS2)
fix(RETS)
fix(RETS)
RETS[1,1]
fix(RETS)
for i = 1 in 100
for ( i = 1 in 100)
for ( i in 1:100)
for (i in 1:100)
for (j in 1:dim(RETS)[1]
)
dim(RETS)[1]
dim(RETS)[2]
for (i in 1:100)
for (j in 1:dim(RETS)[2])
fix(RETS)
for (i in 1:dim(RETS[2])
adj
is.na(RETS[1,2]
)
is.na(RETS[1,1]
is.na(RETS[1,1])
is.na(RETS[1,1])
is.na(RETS[2,2])
for (i in 1:dim(RETS[2])
if (is.na(RETS[3,i])) {
for (i in 1:dim(RETS[2]) {
for (i in 1:dim(RETS[2])
if (is.na(RETS[3,i]))
for (i in 1:dim(RETS[2])) {
if (is.na(RETS[3,i])) {
RETS = RETS[, -i]
}
}
for (i in 1:dim(RETS)[2]) {
if (is.na(RETS[3,i])) {
RETS = RETS[, -i]
}
}
fix(RETS)
load("C:\\Users\\Atranemus-PC\\Documents\\R\\191R\\Final Project\\SP_500_2003_2015.data")
fix(RETS)
RETS[,1]
for (i in 1:dim(RETS)[2]) {
if (is.na(RETS[3, i]))
RETS = RETS[,-i]
}
dim(RETS)[2]
for (i in 1:dim(RETS)[2]) {
if (is.na(RETS[3, i]))
RETS = RETS[,-i]
}
dim(RETS)[2]
for (i in 1:dim(RETS)[2]) {
if (is.na(RETS[3, i]))
RETS = RETS[,-i]
}
fix(RETS)
RETS2 = RETS
dim(RETS2)
RETS3 = na.omit(RETS2)
dim(RETS3)
?cor
cor(RETS3, method = "pearson")
PCOR = cor(RETS3, method = "pearson")
fix(PCOR)
SCOR = cor(RETS3, method = "spearman")
fix(SCOR)
?ace
library('acepack')
install.packages('acepack')
library('acepack')
?ace
?Hmisc
?hoeffd
library('Hmisc')
install.packages('Hmisc')
MaxCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(MaxCOR) = colnames(RETS);
rownames(MaxCOR) = colnames(RETS);
for (i in 1:numStock) {
for(j in 1:numStock) {
transfVars = ace(RETS[,i],RETS[,j]);
Max_cor[i,j] = cor(transfVars$tx,transfVars$ty)[1];
}
}
numStock = dim(RETS)[2]
MaxCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(MaxCOR) = colnames(RETS);
rownames(MaxCOR) = colnames(RETS);
for (i in 1:numStock) {
for(j in 1:numStock) {
transfVars = ace(RETS[,i],RETS[,j]);
Max_cor[i,j] = cor(transfVars$tx,transfVars$ty)[1];
}
}
fix(MaxCOR)
for (i in 1:numStock) {
for(j in 1:numStock) {
transfVars = ace(RETS[,i],RETS[,j]);
MaxCOR[i,j] = cor(transfVars$tx,transfVars$ty)[1];
}
}
ls()
tempRETS = RETS
RETS = RETS3
for (i in 1:numStock) {
for(j in 1:numStock) {
transfVars = ace(RETS[,i],RETS[,j]);
MaxCOR[i,j] = cor(transfVars$tx,transfVars$ty)[1];
}
}
MaxCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(MaxCOR) = colnames(RETS);
rownames(MaxCOR) = colnames(RETS);3
MaxCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(MaxCOR) = colnames(RETS);
rownames(MaxCOR) = colnames(RETS);
for (i in 1:numStock) {
for(j in 1:numStock) {
transfVars = ace(RETS[,i],RETS[,j]);
MaxCOR[i,j] = cor(transfVars$tx,transfVars$ty)[1];
}
}
fix(MaxCOR)
?write
write(MaxCor, file = "MaxCor")
write(MaxCOR, file = "MaxCor")
write(MaxCOR, file = "MaxCor.data")
load("C:\\Users\\Atranemus-PC\\Documents\\R\\191R\\Final Project\\MaxCor.data")
write.table(MaxCOR, file = "mCor.data")
load("C:\\Users\\Atranemus-PC\\Documents\\R\\191R\\Final Project\\mCor.data")
DCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(D_cor) = colnames(RETS);
rownames(D_cor) = colnames(RETS);
#install.packages('energy');
for (i in 1:numStock) {
for(j in 1:numStock) {
DCOR[i,j] = dcor(RET[,i],RET[,j]);
}
DCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(DCOR) = colnames(RETS);
rownames(DCOR) = colnames(RETS);
#install.packages('energy');
for (i in 1:numStock) {
for(j in 1:numStock) {
DCOR[i,j] = dcor(RET[,i],RET[,j]);
}
}
install.packages('energy')
DCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(DCOR) = colnames(RETS);
rownames(DCOR) = colnames(RETS);
#install.packages('energy');
for (i in 1:numStock) {
for(j in 1:numStock) {
DCOR[i,j] = dcor(RET[,i],RET[,j]);
}
}
library('energy')
DCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(DCOR) = colnames(RETS);
rownames(DCOR) = colnames(RETS);
#install.packages('energy');
for (i in 1:numStock) {
for(j in 1:numStock) {
DCOR[i,j] = dcor(RET[,i],RET[,j]);
}
}
DCOR = matrix(, nrow = numStock, ncol = numStock);
colnames(DCOR) = colnames(RETS);
rownames(DCOR) = colnames(RETS);
#install.packages('energy');
for (i in 1:numStock) {
for(j in 1:numStock) {
DCOR[i,j] = dcor(RETS[,i],RETS[,j]);
}
}
fix(DCOR)
install.packages('minerva');
fix(PCOR)
MICCOR = mine(RETS,n.cores = 4)$MIC
library('minerva')
MICCOR = mine(RETS,n.cores = 4)$MIC
save.image("C:\\Users\\Atranemus-PC\\Documents\\R\\191R\\Final Project\\image.RData")
q()
